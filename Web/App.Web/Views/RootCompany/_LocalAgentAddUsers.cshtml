@model UserAgentDto


<form asp-controller="RootCompany" asp-action="" method="post">
    <div class="modal fade" id="localaddUser" tabindex="-1" data-backdrop="false">
        <div class="modal-dialog" style="width:450px;">
            <div class="modal-content">
                <div class="modal-header">
                    <b class="modal-title" id="addRoleLabel">Add Users into Local Agent</b>
                    @* <button type="button" class="close" data-bs-dismiss="modal">
                    <span>X</span>
                    </button>*@
                </div>

                <div class="modal-body" style="overflow-y: scroll;max-height: 500px;">
                    <input type="hidden" asp-for="@Model.foreignAgentId" />
                    <div class="form-group">
                        @for (int i = 0; i < Model.usersAgents.Count; i++)
                        {

                            <div class="form-check m-1">
                                <input type="hidden" asp-for="@Model.usersAgents[i].UserId" />

                                <input asp-for="@Model.usersAgents[i].IsSelected" class="form-check-input" />
                                <label class="form-check-label" style="margin-right:20px !important" asp-for="@Model.usersAgents[i].IsSelected">
                                    @Model.usersAgents[i].userName
                                </label>
                            </div>

                        }
                        <div asp-validation-summary="All" class="text-danger"></div>
                    </div>

                </div>

                <div class="modal-footer">

                    <button type="button" class="btn btn-default" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" name="submit" asp-action="LocalAgentAddUsers" class="btn btn-primary">Save</button>
                </div>
            </div>
        </div>
    </div>
</form>
